package golisp2

// note (bs): out of sheer laziness I'm going to for now hardcode everyting to
// plain string outputs and inputs. That's pretty terrible in a lot of ways, but
// is fine for now.

// ExecString executes the lisp program contained in str, and returns the
// output.
func ExecString(str string) (string, error) {
	// alright, so let's see about creating a

	return "", nil
}
